from django.apps import AppConfig


class AutomationConfig(AppConfig):
    """App configuration for automation utilities and logging."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.automation'
